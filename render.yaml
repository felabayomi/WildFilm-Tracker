services:
  - type: web
    name: wildfilm-tracker-api
    runtime: node
    plan: starter
    buildCommand: npm install --no-audit --no-fund && npm run server:build
    startCommand: npm run server:prod
    autoDeploy: true